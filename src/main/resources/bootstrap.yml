spring:
  application:
    name: configsvc
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:D:\project\my_project\airtms\mycloud\config-server\config-repo
          #search-locations: classpath:/server-configs


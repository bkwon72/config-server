spring:
  application:
    name: configsvc
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:${user.dir}\config-repo
          #search-locations: classpath:/server-configs

